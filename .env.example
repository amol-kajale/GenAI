# Copy to .env and fill in values
# Either `HF_TOKEN` (router token) or `HUGGINGFACE_API_KEY` will be used
HF_TOKEN=hf_REPLACE_WITH_KEY
HUGGINGFACE_API_KEY=hf_REPLACE_WITH_YOUR_TOKEN
HF_DEFAULT_MODEL=meta-llama/Llama-2-7b-chat-hf
# Cost per 1k tokens (adjust based on your HF plan; default is free tier)
COST_PER_1K_PROMPT=0.0
COST_PER_1K_COMPLETION=0.0
